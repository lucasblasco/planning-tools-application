import styled from "styled-components";

import Box from "@mui/material/Box";

export const ButtonToolbar = styled(Box)`
  display: flex;
  justify-content: center;
  gap: 1rem;
  padding: 0.5rem;
`;
